var eeModal = (function () {
    var body, modal;

    var methods = {
        init: init
    };

    function init() {
        body = document.getElementsByTagName('BODY')[0];
        console.log(body);
    }

    var Modal = function () {
        
    };

    return methods;
}());